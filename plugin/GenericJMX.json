{
    "memory": {
        "legend": {
            "committed": {
                "name": "Committed"
            },
            "init": {
                "name": "Init"
            },
            "max": {
                "name": "Total"
            },
            "used": {
                "name": "Used"
            }
        },
        "title": "{{TC}} {{PI}} for {{PC}} on {{HOST}}",
        "vertical": "{{ND}}"
    },
    "memory-heap": {
        "legend": {
            "heap-committed": {
                "name": "Committed"
            },
            "heap-init": {
                "name": "Init"
            },
            "heap-max": {
                "name": "Total"
            },
            "heap-used": {
                "name": "Used"
            }
        },
        "title": "{{TC}} {{PI}} for {{PC}} on {{HOST}}",
        "vertical": "{{ND}}"
    },
    "memory-nonheap": {
        "legend": {
            "nonheap-committed": {
                "name": "Committed"
            },
            "nonheap-init": {
                "name": "Init"
            },
            "nonheap-max": {
                "name": "Total"
            },
            "nonheap-used": {
                "name": "Used"
            }
        },
        "title": "{{TC}} {{PI}} for {{PC}} on {{HOST}}",
        "vertical": "{{ND}}"
    }
}
